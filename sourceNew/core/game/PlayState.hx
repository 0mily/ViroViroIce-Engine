package sourceNew.core;

import flixel.FlxSubState;
import flixel.util.FlxSort;
import flixel.util.FlxStringUtil;
import flixel.util.FlxSave;
import flixel.input.keyboard.FlxKey;
import flixel.animation.FlxAnimationController;
import lime.utils.Assets;
import openfl.utils.Assets as OpenFlAssets;
import openfl.events.KeyboardEvent;
import haxe.Json;

#if !flash
import openfl.filters.ShaderFilter;
#end

class PlayState extends ScriptedState {

    // aqui a gente provavelmente iria deixar como a gameplay base funciona. E sla, coisa como eventos, achievements e essas buceta que ENCHE o playstate, nois faz em outro lugar, morô?

}
