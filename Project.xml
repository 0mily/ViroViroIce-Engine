<?xml version="1.0" encoding="utf-8"?>
<project>
	<!-- _________________________ Application Settings _________________________ -->

	<app unless="mobile" title="Friday Night Funkin': ViroViroIce" file="ViroViroIce" packageName="com.viroviro.ice" package="com.viroviro.ice" main="Main" version="0.1.0" company="BFMix2 Inc." />

	<app if="mobile" title="ViroViroIce" file="ViroViroIce" packageName="com.viroviro.ice" package="com.viroviro.ice" main="Main" version="0.1.0" company="BFMix2 Inc." />
	
	<!--Switch Export with Unique ApplicationID and Icon-->
	<set name="APP_ID" value="0x0100f6c013bbc000" />

	<config type="android" gradle-version="8.10.2" gradle-plugin="8.8.0" ndkVersion="27.3" minimum-sdk-version="26" target-sdk-version="35" />

	<!-- NOTE TO SELF: DISABLE THIS IF ISSUES ARE FOUND -->
	<haxedef name="LINC_LUA_RELATIVE_DYNAMIC_LIB"/> <!-- stable luas PUT AFTER FIRST LINE WITH APP NAME AND ETC -->
	
	<!-- <app preloader="Preloader" resizable="true" /> -->
	<app preloader="flixel.system.FlxPreloader" />

	<!--Minimum without FLX_NO_GAMEPAD: 11.8, without FLX_NO_NATIVE_CURSOR: 11.2-->
	<set name="SWF_VERSION" value="11.8" />
	
	<!-- ______________________ PSYCH ENGINE CUSTOMIZATION ______________________ -->

	<define name="ADDONS_ALLOWED" />
	<define name="HSCRIPT_ALLOWED" />
	<define name="LUA_ALLOWED" if="cpp" />
	<define name="ACHIEVEMENTS_ALLOWED" />
	<define name="DISCORD_ALLOWED" if="cpp" unless="mobile" />
	<define name="TRANSLATIONS_ALLOWED" />
	<define name="IRIS_DEBUG" />
	<define name="EMIMOD"/>
	<define name="flxanimate" />
	
	<define name="INCLUDE_ALL_CLASSES" /> <!-- Removes DCE and compiles (most) classes, for use in scripting -->
	<!-- multirhead is option now bozo -->
	<define name="SHOW_LOADING_SCREEN" />
	<define name="PSYCH_WATERMARKS"/> <!-- DELETE THIS TO REMOVE THE PSYCH LOGO FROM LOADING SCREEN -->

	<section unless="debug">
		<!-- Adds the ability to use OpenFL asset lookup (defaults to sys if disabled)  -->
		<define name="OPENFL_LOOKUP" />

		<!-- Adds the ability to use NativeFileSystem on the computer to acsess files  -->
		<define name="NATIVE_LOOKUP" if="ADDONS_ALLOWED" />
	</section>

	<define name="TOUCH_CONTROLS_ALLOWED" if="mobile || debug" />

	<!-- define name="UNHOLYWANDERER04" -->
	<!-- define name="BASE_GAME_FILES" -->
	<define name="VIDEOS_ALLOWED" unless="32bits" />

	<!-- ____________________________ Window Settings ___________________________ -->

	<!--These window settings apply to all targets-->
	<window width="1280" height="720" fps="60" background="#000000" hardware="true" vsync="false" allow-high-dpi="true" />

	<!--HTML5-specific-->
	<window if="html5" resizable="true" />

	<!--Desktop-specific-->
	<window if="desktop" orientation="landscape" fullscreen="false" resizable="true" vsync="false"/>

	<!--Mobile-specific-->
	<window if="mobile" orientation="landscape" fullscreen="true" resizable="false" allow-shaders="true" require-shaders="true" />

	<!--Switch-specific-->
	<window if="switch" orientation="landscape" fullscreen="true" width="0" height="0" resizable="true" />

	<!-- _____________________________ Path Settings ____________________________ -->

	<set name="BUILD_DIR" value="export/debug" if="debug" />
	<set name="BUILD_DIR" value="export/release" unless="debug" />
	<set name="BUILD_DIR" value="export/32bit" if="32bits" />

	<classpath name="source" />
	<java if="android" path="source/external/android/java" />

	<!-- __________________________ Game Assets & Etc. __________________________ -->
	
	<!-- Turn on Crash Handler -->
	<define name="CRASH_HANDLER" if="release" />

	<!-- Assets -->
	<assets path="assets/fonts" />
	<assets path="assets/shared"        exclude="*.mp3"/>
	<assets path="assets/songs"         exclude="*.mp3"/>
	<assets path="assets/embed"         embed="true"/>
	<assets path="assets/videos"        if="VIDEOS_ALLOWED"/>
	<assets path="assets/week_assets"   rename="assets" exclude="*.mp3"/>
	
	<section if="TITLE_SCREEN_EASTER_EGG">
		<assets path="assets/secrets" rename="assets/shared"/>
	</section>

	<section if="TRANSLATIONS_ALLOWED">
		<assets path="assets/translations" rename="assets" exclude="*.mp3"/>
	</section>

	<section if="BASE_GAME_FILES">
		<assets path="assets/base_game" rename="assets" exclude="*.mp3"/>
	</section>
	
	<section if="ADDONS_ALLOWED">
		<assets path='example_addons' rename='addons' embed='false' type='template' unless="mac || mobile"/>
		<assets path='example_addons' rename='addons' embed='false' if="mac || mobile"/>
		<assets path='example_contents' rename='contents' embed='false' type='template' unless="mac || mobile"/>
		<assets path='example_contents' rename='contents' embed='false' if="mac || mobile"/>
		<assets path='list.txt' rename='addonsList.txt' />
	</section>

	<define name="CHECK_FOR_UPDATES" if="officialBuild"/>

	<assets path='art/readme.txt' rename='do NOT readme.txt' />

	<!-- OpenAL config -->
	<section if="desktop">
		<assets path="alsoft.txt" rename="plugins/alsoft.ini" type="text" if="windows"/>
		<assets path="alsoft.txt" rename="plugins/alsoft.conf" type="text" unless="windows"/>
	</section>
	
	<!-- __________________________________ Fun _________________________________ -->
	
	<define name="SCRIPTS_ALLOWED" if="HSCRIPT_ALLOWED || LUA_ALLOWED" />
	
	<section if="INCLUDE_ALL_CLASSES SCRIPTS_ALLOWED">
		<haxeflag name="-dce no" />
		<haxeflag name="--macro" value="backend.macro.Init.includeClasses()" />
	</section>
	
	<haxeflag name="--macro" value="backend.macro.Init.init()" />

	<!-- _______________________________ Libraries ______________________________ -->

	<haxelib name="flixel" />
	<haxelib name="flixel-addons" />
	<haxelib name="nape-haxe4" /> <!-- bf doin -->
	<haxelib name="extension-haptics" if="mobile" />
	<haxelib name="extension-androidtools" if="android" />
	<haxelib name="tjson" />

	<!--Psych stuff needed-->
	<section if="VIDEOS_ALLOWED" unless="html5">
		<haxelib name="hxCodec" if="linux" /> <!-- fuck my stupid baka chud linux life -->
		<haxelib name="hxvlc" unless="linux" />
	</section>
	<haxelib name="linc_luajit" if="LUA_ALLOWED"/>
	<haxelib name="hscript-iris" if="HSCRIPT_ALLOWED" />
	<haxelib name="hxdiscord_rpc" if="DISCORD_ALLOWED" />
	<haxelib name="flixel-animate" />
	<haxelib name="moonchart" />
	<haxelib name="flxgif" />
	<haxelib name="yagp" />
	
	<!-- Other -->
	<haxelib name="grig.audio" />
	<haxelib name="funkin.vis" />

	<haxedef name="FM_ENGINE" value="PSYCH"/>
	<haxedef name="FM_ENGINE_VERSION" value="1.0"/>

	<haxelib name="funkin-modchart" />
	<haxeflag name="--macro" value="modchart.backend.macros.Macro.includeFiles()"/>

	<!-- Disable Discord IO Thread -->
	<haxedef name="DISCORD_DISABLE_IO_THREAD" if="hxdiscord_rpc" />
	<haxedef name="NO_PRECOMPILED_HEADERS" />

	<!-- Enables a terminal log prompt on debug builds -->
	<haxelib name="hxcpp-debug-server" if="debug" />
	<haxedef name="HXC_LIBVLC_LOGGING" if="VIDEOS_ALLOWED debug" />
	<haxedef name="HXVLC_NO_SHARE_DIRECTORY" if="VIDEOS_ALLOWED" />
	<define name="x86_BUILD" if="32bits" />
	
	<!-- ______________________________ More defines _____________________________ -->
	
	<!--Disable the Flixel core focus lost screen-->
	<haxedef name="FLX_NO_FOCUS_LOST_SCREEN" />
	
	<!-- Show debug traces for hxCodec -->
	<haxedef name="HXC_DEBUG_TRACE" if="debug" />
	
	<!--Disable the Flixel core debugger. Automatically gets set whenever you compile in release mode!-->
	<haxedef name="FLX_NO_DEBUG" unless="debug" />

	<!--Enable this for Nape release builds for a serious peformance improvement-->
	<haxedef name="NAPE_RELEASE_BUILD" unless="debug" />
	
	<!--Shuts Flixel up-->
	<haxedef name="FLX_COVERAGE_TEST" />
	
	<!-- i'm cumming btw -->
	<haxedef name="HXCPP_CHECK_POINTER" if="HXCPP_HEAVY_CRASH_DIAGNOSTICS" />
	<haxedef name="HXCPP_STACK_LINE" if="HXCPP_HEAVY_CRASH_DIAGNOSTICS" />
	<haxedef name="HXCPP_STACK_TRACE" if="HXCPP_HEAVY_CRASH_DIAGNOSTICS" />
	
	<!--Disable deprecated warnings-->
	<haxedef name='no-deprecation-warnings' />

	<!-- Haxe 4.3.0+: Enable pretty syntax errors and stuff. -->
	<!-- pretty (haxeflixel default), indent, classic (haxe compiler default) -->
	<haxedef name="message.reporting" value="pretty" />
	
	<!--HScript-->
	<haxedef name="hscriptPos" if="HSCRIPT_ALLOWED" />
	<define name="GLOBAL_SCRIPTS" if="HSCRIPT_ALLOWED" />

	<section if="android">
		<certificate path="key.keystore" password="viroviroice" alias="viroviroice" alias-password="viroviroice" if="android" unless="debug" />
	</section>

	<!-- _________________________________ Custom _______________________________ -->
	
	<section if="mobile">
		<config type="android" viroviro-mono-icon="art/icons/mobile/iconMono.png" /> <!-- espero arranjar um jeito de não precisar fazer um fork da porra do lime pra fazer isso funfar -->
		<!-- <icon path="art/icons/mobile/iconAdaptive.png" if="android" /> focar nisso depois -->
		<icon path="art/icons/mobile/iconMobile.png" if="android" />
	</section>
	<section unless="mobile">
		<assets path='art/icons/desktop/iconOG.png' rename='icon.png' if="linux || mac" />
		<icon path="art/icons/desktop/icon16.png" size='16' />
		<icon path="art/icons/desktop/icon32.png" size='32' />
		<icon path="art/icons/desktop/icon64.png" size='64' />
		<icon path="art/icons/desktop/iconOG.png" />
	</section>
</project>
